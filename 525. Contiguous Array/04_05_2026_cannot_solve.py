# problem: https://leetcode.com/problems/contiguous-array/
# spent 32 min, but couldn't solve this problem.


# i first assumed O(n) time solution based on the constraints, so i approached with dp, two-pointer, and sliding window algorithms. however, i wasn't able to come up with a solution.


class Solution:
    def findMaxLength(self, nums: List[int]) -> int:
